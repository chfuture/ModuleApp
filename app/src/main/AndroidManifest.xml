<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ycwang.moduleapp">

    <application
        android:allowBackup="true"
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragment.ViewPagerActivity" />
        <activity android:name=".annotation.AnnotationActivity" />
        <activity android:name=".dispatchEvent.activity.view.ModuleMainActivity" />
        <activity android:name=".dispatchEvent.activity.ModuleManagerActivity" />
    </application>

</manifest>